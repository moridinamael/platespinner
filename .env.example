# Server port (default: 3001)
# PORT=3001

# Railway CLI binary path (default: 'railway' from PATH)
# RAILWAY_BIN=railway

# Enable debug logging for autoclicker judgment agent (default: off)
# DEBUG_AUTOCLICKER=1
